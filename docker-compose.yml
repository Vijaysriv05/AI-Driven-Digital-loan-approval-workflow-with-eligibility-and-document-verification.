version: '3.8'

services:
  loan-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: loan_app_container
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - NODE_ENV=production
    env_file:
      - ./backend/.env
    restart: unless-stopped
